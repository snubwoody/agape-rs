
/// This should be a global error that has some wrappers, and then
/// there should be context specific errors then each error should have
/// a cause that returns the underlying error
struct Error{

}